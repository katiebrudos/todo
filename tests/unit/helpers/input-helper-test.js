import { inputHelper } from '../../../helpers/input-helper';
import { module, test } from 'qunit';

module('Unit | Helper | input helper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = inputHelper(42);
  assert.ok(result);
});
